int leader = 0;
int finished = 0;
while(finished == 0) { 
    msg = receive();
    if(msg == 0) {
        exit(0);
    } else {         
        if(msg == uid()) { 
            leader = msg;
        } else { 
            if(msg > 
    }
 }
